# Railway Nixpacks configuration
# Let Railway auto-detect Node.js - no explicit nixPkgs needed

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npm start"
