# Production Environment Variables Template
# Copy these values to Google Cloud Secret Manager or Cloud Run environment variables
# When deploying to: bldcebu-portal

# Database (Cloud SQL - Production)
# Format: postgresql://postgres:PASSWORD@/DATABASE_NAME?host=/cloudsql/CONNECTION_NAME
DATABASE_URL=postgresql://postgres:PASSWORD@/bld_portal?host=/cloudsql/bldcebu-portal:asia-southeast1:bld-portal-db

# JWT Secrets (Store in Secret Manager as: prod-jwt-secret, prod-jwt-refresh-secret)
# Generate strong random strings: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Use DIFFERENT secrets than development!
JWT_SECRET=GENERATE_STRONG_RANDOM_STRING_DIFFERENT_FROM_DEV
JWT_REFRESH_SECRET=GENERATE_STRONG_RANDOM_STRING_DIFFERENT_FROM_DEV

# API Configuration
API_PREFIX=api/v1
PORT=4000

# Frontend URL (Production domain)
FRONTEND_URL=https://yourdomain.com

# Node Environment
NODE_ENV=production

# Optional: BunnyCDN (for QR code storage)
# BUNNY_CDN_STORAGE_ZONE=
# BUNNY_CDN_ACCESS_KEY=
# BUNNY_CDN_PULL_ZONE_URL=
