# Production Environment Variables Template
# Use these values when deploying frontend to Cloud Run (prod environment)
# When deploying to: bldcebu-portal

# Backend API URL (Production Cloud Run backend URL)
# Replace XXXXX with your actual Cloud Run service URL
# Get URL with: gcloud run services describe bld-portal-backend --region asia-southeast1 --format="value(status.url)"
NEXT_PUBLIC_API_BASE_URL=https://bld-portal-backend-XXXXX-uc.a.run.app
NEXT_PUBLIC_API_URL=https://bld-portal-backend-XXXXX-uc.a.run.app/api/v1

# Node Environment
NODE_ENV=production
